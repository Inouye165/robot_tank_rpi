# Local model files for the optional OpenCV ONNX vision backend.
#
# This directory is gitignored except for this README and a `.gitkeep`.
# Place ONNX YOLO weights here on the Pi (e.g. `models/yolo-nano.onnx`)
# and point `TANK_VISION_MODEL_PATH` at the file.
#
# Models are NOT downloaded automatically and must NOT be committed to the
# repository (they are large and licensed independently).

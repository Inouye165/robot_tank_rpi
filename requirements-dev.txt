-r requirements.txt
pytest==9.0.2
# Optional: enables the OpenCvOnnxDetector backend and the postprocess
# smoke test in tests/test_detectors.py. Not required for the default
# (monitor-only / disabled) cockpit experience.
opencv-python-headless
numpy

ruff==0.11.13